PORT=you can set the port number for your backend server in the .env file. For example, if you want to run your server on port 5001, you can set it as follows:

MONGODB_URI=you can set the MongoDB connection URI in the .env file. For example, if you are using MongoDB Atlas, you can set it as follows:
JWT_SECRET=your secret key for JWT authentication. You can set it in the .env file as follows:
NODE_ENV=development